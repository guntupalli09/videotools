# Copy to .env and fill in only what you need. Do not commit .env.
# This file is for repo-root env vars (SEO scripts, etc.). Server/client have their own.

# ---------------------------------------------------------------------------
# Optional: SEO weekly pipeline (npm run seo:weekly)
# Without these, the pipeline still runs using free sources (Google suggest, YouTube, Reddit).
# See docs/seo-api-keys.md for details.
# ---------------------------------------------------------------------------
# SERP_API_KEY=          # SerpApi (serpapi.com) – enable in scripts/seo/seo.config.json
# AHREFS_API_KEY=        # Ahrefs (optional)
# SEMRUSH_API_KEY=       # SEMrush (optional)
# GSC: not implemented. If added later, use Google Search Console API (OAuth or service account JSON path), not a simple key.

# ---------------------------------------------------------------------------
# Optional: sitemap generation (when running scripts/seo/generate-sitemap.ts)
# ---------------------------------------------------------------------------
# SITE_URL=https://www.videotext.io
# SITEMAP_PING=1         # set 0 or false to skip pinging Google/Bing
# SITEMAP_OUTPUT=        # override output path
