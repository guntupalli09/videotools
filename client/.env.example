# Vercel (production): set these in Project Settings → Environment Variables.
# Build-time only (VITE_* are baked into the client bundle).

# Backend API base URL including /api (e.g. https://api.videotext.io/api).
# Required for production so the frontend can reach your Hetzner API.
VITE_API_URL=https://api.videotext.io/api

# Canonical site URL for SEO (canonical, OG, sitemap). No trailing slash.
# Optional; defaults to https://www.videotext.io if unset.
VITE_SITE_URL=https://www.videotext.io
