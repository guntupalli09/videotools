# Vercel (production): set these in Project Settings → Environment Variables.
# Build-time only (VITE_* are baked into the client bundle).

# Backend API ORIGIN ONLY (no /api). All paths are /api/*. Required for production.
# Example: https://api.videotext.io  (NOT https://api.videotext.io/api)
# Local dev: set VITE_API_URL=http://localhost:3001 to talk to the API directly and avoid
# proxy timeouts on large chunked uploads (otherwise the client uses 2 MB chunks, 1 at a time).
VITE_API_URL=https://api.videotext.io

# Canonical site URL for SEO (canonical, OG, sitemap). No trailing slash.
# Optional; defaults to https://www.videotext.io if unset.
VITE_SITE_URL=https://www.videotext.io

# PostHog (optional). Copy this file to .env and set for local dev.
# Required for analytics; without it the client won't send events and dashboards stay at zero.
# VITE_POSTHOG_KEY=phc_xxxxxxxx  (same as POSTHOG_KEY in server/.env)
# VITE_POSTHOG_HOST=https://app.posthog.com
